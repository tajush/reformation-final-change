{% comment %}
  <div class="signup-container">
    {% form 'customer', class: 'signup-form' %}
      <fieldset>
        <input name="contact[tags]" type="hidden" value="newsletter">
        {%- if form.errors -%}
          <div class="form-notification error" id="Newsletter-error--{{ section.id }}">
            {% render 'svg-icons' with 'thb-error' %}
            {{ form.errors.translated_fields.email | capitalize }}
            {{ form.errors.messages.email }}
          </div>
        {%- endif -%}
        {%- if form.posted_successfully? -%}
          <div class="form-notification success" id="Newsletter-success--{{ section.id }}">
            {% render 'svg-icons' with 'thb-success' %}
            {{ 'newsletter.success' | t }}
          </div>
        {%- endif -%}
        <div class="field">
          <input
            class="signup-newsletter"
            placeholder="{{ 'newsletter.label' | t }}"
            type="email"
            id="NewsletterForm--{{ section.id }}"
            name="contact[email]"
            autocorrect="off"
            autocapitalize="off"
            autocomplete="email"
            value="{{ form.email }}"
            pattern="^([a-zA-Z0-9_\-\.]+)@((\[[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\.)|(([a-zA-Z0-9\-]+\.)+))([a-zA-Z]{2,4}|[0-9]{1,10})(\]?)$"
            title="xxx@xxx.xxx"
            {% if form.errors %}
              autofocus
              aria-invalid="true"
              aria-describedby="Newsletter-error--{{ section.id }}"
            {% elsif form.posted_successfully? %}
              aria-describedby="Newsletter-success--{{ section.id }}"
            {% endif %}
            required
          >

          <!--
            <label class="field__label" for="NewsletterForm--{{ section.id }}">
              {{ 'newsletter.label' | t }}
            </label>
          -->
        </div>

        <button class="submit" type="submit" aria-label="{{ 'sections.signup.enter_your_email' | t }}">
          {% render 'svg-icons' with 'signup-submit' %}
        </button>
      </fieldset>

      {%- if settings.email_terms_conditions_enable -%}
        <div class="signup-container--terms">
          <input type="checkbox" id="NewsletterTerms-{{ form.id }}" required>
          <label for="NewsletterTerms-{{ form.id }}">
            {{ 'sections.signup.i_agree' | t }}
          </label>
        </div>
      {%- endif -%}
    {% endform %}
  </div>
{% endcomment %}

<div class="signup-container">
  {% form 'customer', class: 'signup-form' %}
    <fieldset>
      <input name="contact[tags]" type="hidden" value="newsletter">
      {%- if form.errors -%}
        <div class="form-notification error" id="Newsletter-error--{{ section.id }}">
          {% render 'svg-icons' with 'thb-error' %}
          {{ form.errors.translated_fields.email | capitalize }}
          {{ form.errors.messages.email }}
        </div>
      {%- endif -%}
      {%- if form.posted_successfully? -%}
        <div class="message"
          style="
           
          "
        >
          <!-- {% render 'svg-icons' with 'thb-success' %} -->
          <!-- {{ 'newsletter.success' | t }} -->
          <div style="            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 20 20"><path fill="black" fill-rule="evenodd" d="m7.172 11.334l2.83 1.935l2.728-1.882l6.115 6.033q-.242.079-.512.08H1.667c-.22 0-.43-.043-.623-.12zM20 6.376v9.457c0 .247-.054.481-.15.692l-5.994-5.914zM0 6.429l6.042 4.132l-5.936 5.858A1.7 1.7 0 0 1 0 15.833zM18.333 2.5c.92 0 1.667.746 1.667 1.667v.586L9.998 11.648L0 4.81v-.643C0 3.247.746 2.5 1.667 2.5z"/></svg></div>
       <h1>Check your email</h1>
        <p style="font-size:16px";>Thanks for subscribing!</p>
          </div>
      {%- endif -%}

      <div class="field">
        <input
          class="signup-newsletter"
          placeholder="{{ 'newsletter.label' | t }}"
          type="email"
          id="NewsletterForm--{{ section.id }}"
          name="contact[email]"
          autocorrect="off"
          autocapitalize="off"
          autocomplete="email"
          value="{{ form.email }}"
          pattern="^([a-zA-Z0-9_\-\.]+)@((\[[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\.)|(([a-zA-Z0-9\-]+\.)+))([a-zA-Z]{2,4}|[0-9]{1,10})(\]?)$"
          title="xxx@xxx.xxx"
          {% if form.errors %}
            autofocus
            aria-invalid="true"
            aria-describedby="Newsletter-error--{{ section.id }}"
          {% elsif form.posted_successfully? %}
            
            aria-describedby="Newsletter-success--{{ section.id }}"
          {% endif %}
          required
        >

        <!--
          <label class="field__label" for="NewsletterForm--{{ section.id }}">
            {{ 'newsletter.label' | t }}
          </label>
        -->
      </div>

      <button class="submit" type="submit" aria-label="{{ 'sections.signup.enter_your_email' | t }}">
        {% render 'svg-icons' with 'signup-submit' %}
      </button>
    </fieldset>

    {%- if settings.email_terms_conditions_enable -%}
      <div class="signup-container--terms">
        <input type="checkbox" id="NewsletterTerms-{{ form.id }}" required>
        <label for="NewsletterTerms-{{ form.id }}">
          {{ 'sections.signup.i_agree' | t }}
        </label>
      </div>
    {%- endif -%}
  {% endform %}
</div>
<style>
  .message{
     /* padding: 0 89px; */
            z-index: 10000;
            position: absolute;
            background-color: white;
            position: absolute;
            top: -360px;
            /* left: 0; */
            height: 550px;
            width: 400px;
            display: flex;
                      flex-direction: column;

            justify-content: center;
            align-items: center;
            font-size: 30px;
            font-weight: 600;
  }
  @media only screen and (max-width: 600px) {
  .message h1 {
font-size:20px;
  }
    .message{
      width:260px;
    }
}
</style>